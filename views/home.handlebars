<h1>iCalendar Stream EDitor (ical-sed)</h1>

<form>

	<div class="form-group">
		<label for="source">Source URL</label>
		<input id="source" class="form-control" tabindex="0" placeholder="http://example.com/feed.ics" type="text" data-bind="value: source, valueUpdate: 'keyup'">
	</div>

	<div class="form-group">
		<label for="terms">Include Terms</label>
		<div id="terms" data-bind="foreach: terms" class="terms form-horizontal">
			<div class="form-group">
				<div class="col-xs-4 col-md-4">
					<input class="form-control term" type="text" data-bind="value: $rawData, valueUpdate: 'keyup', event: { keypress: $parent.termKeyPress }">
				</div>
				<button class="btn btn-default" data-bind="click: $parent.removeTerm">Remove</a>
			</div>
		</div>
		<button class="btn btn-default" data-bind="click: addTerm">Add</button>				 
	</div>

	<div class="form-group">
		<label for="url">Filtered URL</label>
		<a id="url" class="form-control" data-bind="attr: { href: url }, text: url"></a>
	</div>
	<button class="btn btn-primary" data-clipboard-target="#url">Copy</button>

</form>
